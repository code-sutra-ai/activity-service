# Resilience4j configuration
resilience4j:
  circuitbreaker:
    instances:
      default:
        registerHealthIndicator: true
        slidingWindowSize: 10
        minimumNumberOfCalls: 5
        failureRateThreshold: 50
        waitDurationInOpenState: 10000

# Cognito authentication configuration
# Set these in your environment or secrets manager for production
COGNITO_JWK_URL: ""
COGNITO_ISSUER: ""

# Logging
logging:
  level:
    root: INFO
    org:
      springframework:
        web: INFO

# Prometheus and Actuator configuration
management:
  endpoints:
    web:
      exposure:
        include: health,info,prometheus
  endpoint:
    prometheus:
      enabled: true
  tracing:
    enabled: true
    sampling:
      probability: 1.0
  otlp:
    tracing:
      endpoint: http://localhost:4318/v1/traces
      export:
        enabled: true
  metrics:
    export:
      prometheus:
        enabled: true